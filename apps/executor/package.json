{
  "name": "@coderelay/executor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "DOTENV_CONFIG_PATH=../../.env tsx watch src/worker.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/worker.js",
    "test": "vitest run",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "@kubernetes/client-node": "^1.0.0",
    "@prisma/client": "^6.0.1",
    "bullmq": "^5.34.6",
    "ioredis": "^5.4.1"
  }
}
